// src/context/BoardContextObject.jsx
import { createContext } from "react";

//BoardContext is the React Context object for boards, columns, and cards
 
export const BoardContext = createContext(null);